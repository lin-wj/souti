export const config = {
  // ── 抽帧控制 ──────────────────────────────────────────────
  /** 本地抽帧间隔（毫秒）。摄像头 30fps，此处约每 200ms 采样一帧用于检测。 */
  FRAME_INTERVAL: 200,

  /**
   * 连续相似帧确认数。
   * 在 CONTENT_DETECTED 后，需要连续 N 帧相似才触发识别。
   * 值越小触发越快，但可能误触；值越大越稳，但响应慢。
   * 推荐 3–5（200ms 间隔下对应 600–1000ms）。
   */
  STABLE_FRAME_COUNT: 3,

  /**
   * 画面变化检测阈值（0–1）。
   * 两帧之间差异超过此值时判定为"画面有变化"，重置稳定计数。
   * 值越小越敏感，越大越宽松。
   * 推荐 0.08–0.12（允许手持轻微晃动）。
   */
  CHANGE_THRESHOLD: 0.10,

  /** 两次识别之间的冷却时间（毫秒），防止快速连续触发。 */
  COOLDOWN_TIME: 2000,

  // ── 图片处理 ──────────────────────────────────────────────
  /** 截取识别框后缩放的宽度目标像素。高度等比例缩放。 */
  TARGET_WIDTH: 768,

  /** 上传前 JPEG 压缩质量（0–1）。影响图片尺寸与识别精度之间的平衡。 */
  IMAGE_QUALITY: 0.85,

  /** 识别框在画面中的相对宽度（占总画面宽度的比例）。 */
  FRAME_WIDTH_RATIO: 0.85,

  /** 识别框在画面中的相对高度（占画面高度的比例）。 */
  FRAME_HEIGHT_RATIO: 0.65,

  // ── AI 请求 ───────────────────────────────────────────────
  /** AI 请求超时（毫秒）。超过此时间视为超时。 */
  AI_TIMEOUT_MS: 30000,

  // ── 调试 ──────────────────────────────────────────────────
  /** 是否启用调试面板（URL 参数 ?debug=1 可覆盖）。 */
  DEBUG_DEFAULT: false,
};

export default config;
