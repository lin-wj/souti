export const config = {
  // ── 抽帧控制 ──────────────────────────────────────────────
  /** 本地抽帧间隔（毫秒）。摄像头 30fps，此处约每 500ms 采样一帧用于检测。 */
  FRAME_INTERVAL: 500,

  /** 画面稳定所需最短持续时间（毫秒）。检测到稳定后等待该时长再触发识别。 */
  STABLE_DURATION: 800,

  /**
   * 画面变化检测阈值（0–1）。
   * 两帧之间差异超过此值时判定为"画面有变化"。
   * 值越小越敏感，越大越宽松。
   */
  CHANGE_THRESHOLD: 0.06,

  /** 两次 AI 请求之间的冷却时间（毫秒），防止快速连续触发。 */
  COOLDOWN_TIME: 2000,

  // ── 图片处理 ──────────────────────────────────────────────
  /** 截取识别框后缩放的宽度目标像素。高度等比例缩放。 */
  TARGET_WIDTH: 768,

  /** 上传前 JPEG 压缩质量（0–1）。影响图片尺寸与识别精度之间的平衡。 */
  IMAGE_QUALITY: 0.85,

  /** 识别框在画面中的相对宽度（占总画面宽度的比例）。 */
  FRAME_WIDTH_RATIO: 0.7,

  /** 识别框在画面中的相对高度（占画面高度的比例）。 */
  FRAME_HEIGHT_RATIO: 0.5,

  // ── AI 请求 ───────────────────────────────────────────────
  /** AI 请求超时（毫秒）。超过此时间视为超时。 */
  AI_TIMEOUT_MS: 30000,

  // ── 调试 ──────────────────────────────────────────────────
  /** 是否启用调试面板（URL 参数 ?debug=1 可覆盖）。 */
  DEBUG_DEFAULT: false,
};

export default config;
